- Let the person re-tagging say what the model is missing, instead of only saying no. `sypy retag <id>` offered accept, regenerate, or cancel, so the only thing a refusal carried was the path it turned down — and a document the model keeps reading as clinical when it is mathematical takes as many requests to walk away from that reading as you are willing to pay for, because nothing in the loop could tell it. `s` now asks for a sentence — "it is about the maths, not the clinic", "file it near the tax papers" — and the next round is asked under it. It is placed last in the prompt and marked as outranking everything above it, the rejected list included: the person has read the document and the model has read its first pages, and a steer that walks back a path turned down two rounds ago is exactly the correction that rule would otherwise forbid. The latest sentence replaces the one before it rather than accumulating, since someone correcting their own steer means the newer one and two contradictory instructions in a prompt are worth less than either alone. What was asked for is printed beside the answer it produced, so a suggestion that ignored it is visibly that. An empty answer is not a decision: the menu comes back and no request is spent.
- Install the command under `sypy` as well as its full name. The rename gave the distribution, the package, and the command one word, which was the point, and left a 15-character command to type at the start of every line. Both names are now entry points of the same package, so a `pip install sortyourpaperya` from anywhere provides both, and `install.sh` links both into `~/.local/bin`. A symlink rather than a shell alias, because a script, the background service, and an agent all reach for a command on PATH and none of them read your rc file. `unwire` takes each name off on its own: a `sypy` someone else wrote is theirs to keep, and refusing over it no longer strands our own link on PATH — it is still reported, and still an error, once everything that is ours has come off. Anyone who removed `~/.local/bin/sypy` on the previous release, as it said to, gets it back by re-running `./install.sh`.
- Rename the project, the package, and the command to `sortyourpaperya`. `sypy` was three identities at once — the distribution, the import package, and the command — sitting under a project called SortYourPapers, so the name you typed was not the name of the thing you had installed. All of it is now one word: `pip install sortyourpaperya`, `import sortyourpaperya`, `sortyourpaperya find ...`, `SORTYOURPAPERYA_*` for every environment variable, and the agent skill under the same name. State moves with it — `~/.config/sypy`, `~/.local/state/sypy`, and the log folder become `sortyourpaperya`, carrying the watch registry, the recorded spend, and the watch locks across rather than starting them again. The launchd label was `com.sortyourpapers.sypy`, two names for the same project; it is now `com.sortyourpaperya.watcher`, which says what the service is instead of saying the project twice. This changelog and `docs/archive/` are left spelling the old name, because they record what was true when they were written and a history rewritten to claim a name it never had is not a history. An existing install keeps a `sypy` on PATH pointing into a package that no longer exists: remove `~/.local/bin/sypy` and re-run `./install.sh`.
- Let a note be called whatever its owner calls it. A note was `notes.md` and only `notes.md`, so a second one about the same document — a reading log, a table pulled out of it, anything a program wanted to write back and read again — had nowhere to go that the library would admit existed. Any markdown or JSON file in a document's folder is now a note: `sypy note <id> reading-log` opens one by name, a bare word is markdown so `.md` need not be typed, and `extracted.json` is created as `{}` rather than as a markdown heading, which is broken for the only thing JSON is kept for. A suffix that is neither is refused rather than corrected, since renaming it files it under a name its owner will not look for, and the document itself is never reported as a note about itself — a contract filed as JSON sits in the same folder as the notes about it. `find --json` and `list --json` records carry `notes` as the list of notes that exist, where it was one path that might not; a reader that cannot see the note beside a document writes a second copy of it. Naming one is only needed once there is more than one: a document with a single note opens it whatever it is called, and a document with several lists them and stops rather than guessing which was meant.
- Open the database to whatever is reading the library, which until now saw about half of it. `find --json` and `list --json` records now carry `filed_at`, `updated_at`, `size_bytes`, `pages_read`, and `attributes`; both commands take `--sort id|recent|updated|title|year|size`, where before every result came back in `file_id` order — a hash — so "the three most recent" was not a question the library could be asked at all. `sypy categories` groups the category paths with a count under each, which is what a re-tag should be decided from and what `list --json` was being dumped whole to work out. `sypy attr <id> [key] [value]` reads and writes the `paper_attributes` table, which has existed since the first migration with nothing able to reach it: free key/value pairs for what the library has no column for and the model was never asked — a DOI, a venue, a verdict, the day it was read. They are deliberately outside what `upsert` rewrites, so a rescan, a re-tag, and a re-ingest of the same document all leave them untouched and only removing the document removes them, which is what makes them somewhere a reader can put a finding and expect to find it again. `sypy sql "<statement>"` is the rest: `content_hash`, `stored_mtime_ms`, and the `page_text` banked in `model_answers` — text already extracted and already paid for, which a reader was otherwise re-parsing the PDF to get. Only a single reading statement is accepted, checked against the statement with its comments stripped and its string bodies blanked so neither a `--` nor a literal can hide a second one; that is a guard against a query written to count documents deleting them, not a permission boundary. Sorts name a clause and never supply one. Opening `papers.duckdb` directly is not the alternative it looks like: DuckDB allows one process and refuses a second connection even read-only, so it works only while no watcher is running. The agent skill documents all of it, including that.
- Say what the watcher filed and where, on screen as well as in the log. Every pass now logs one line per document — `filed vaswani_2017_attention-is-all-you-need under AI/Transformers` — where before it logged only a count, and a count is not something anyone can check: the category is the part that can be wrong, and nothing said what it was. A pass that filed something also sends a desktop notification naming the first three documents and their categories, because a watcher running as a service leaves nothing but a log file nobody is reading while it runs, and a mis-filed document is worth catching while it is one rather than fifty. `osascript` on macOS, `notify-send` on Linux, and nothing at all on a machine with neither — the text is passed as arguments rather than built into a script, so a title or a category the model chose cannot change what is run, and every failure is swallowed: being told is a convenience, filing is the job. Preview mode announces nothing, having filed nothing.
- Answer a bare `sypy` with the full help instead of "Missing command". The error named neither the commands there are nor the one that was missing, which is the whole of what someone typing the command with nothing after it is asking.
- Put each document in the tree as one symlink again, dropping the real folder that held it. `tree/<tags>/<name>/<name> -> store/...` becomes `tree/<tags>/<name> -> store/...`: the wrapper existed so that opening a document from a category left you in the tree rather than in the store, and it cost a level of nesting and a doubled name on every path, and left real directories throughout a part of the library that is disposable. A document whose place is taken by a real file still steps aside to an id-decorated name beside it, and `sypy tree` still reports whatever is sitting there. Nothing in the store or the database moves, so an existing library converges on the next `sypy tree`.
- Let a document be named by words rather than by its id. `retag`, `note`, and `remove` now resolve their argument through the same search `sypy find` uses — ids, titles, filenames, years, tags, authors, keywords — so `sypy note kahn` finds what `sypy note 78c64b3b8ef6` did. An exact id is never searched for, so a document can always be named unambiguously even when its id appears in another document's keywords. Several matches open `fzf` when it is installed and there is a terminal to draw on, with the id carried on each line but hidden from the display by `--with-nth`; without a picker the matches are printed and nothing is done, so the behaviour is the same scripted as it is by hand and never a guess. What a word resolved to goes to stderr, leaving `$(sypy note kahn --path)` yielding only the path. `sypy remove --yes` refuses a word and wants an exact id: the confirmation is what shows you which document a word found, `--yes` is the flag that skips it, and which document a word matches changes as the library grows.
- Log this package's own lines at INFO and leave everything else at WARNING, instead of turning the whole root logger up. The HTTP client under the OpenAI SDK logs a line per request at INFO, which says nothing the tool does not already report and which landed in the middle of `sypy retag`'s prompt while it waited for an answer. Named by what is ours rather than by what is noisy, because the SDK vendors its HTTP client: that logger is `httpx2` today and whatever the next release vendors tomorrow, so silencing it by name would go stale. Third-party warnings and errors still come through, and `-v` brings all of it back.
- Add a way back from a category steering got wrong. `sypy retag <id>` with no category asks the model where the document belongs, shows what it proposes, and writes nothing until you accept; `r` asks again, `c` cancels. The re-ask is deliberately not the question ingest asks. Ingest is told to *prefer* an existing path, which is exactly what files a computational cognitive science paper under `Psychology/Research Methods` when the library holds two research-methods documents and nothing closer — so here the existing paths are context to weigh, and every round carries the categories already turned down. Without that last part the inputs would be identical each time and "give me another" would return the same answer forever. Accepting replaces tags and keywords in one transaction, since taking the model's category and keeping its old keywords would describe the document as two things at once; a hand-typed re-tag still leaves keywords alone, and title, authors, and year are never touched because they name the link. What the model reads is the library's own record followed by the document's first pages, so a scan and a document whose file has gone missing both still get an answer with no second request. The database is released before the first request: the exchange waits on a person, and holding the write lock across that would stop the watcher, which waits 30 seconds and then fails. An end of input is a cancel rather than a traceback, so running it with stdin closed stops cleanly.
- Stop calling the tool a prototype. `prototype/` becomes `python/`, the package `syp_prototype` becomes `sypy` — one name to learn, the same one the command has always had — and the distribution `syp-prototype` becomes `sypy`. The framing was left over from when this was the front half of a Rust pipeline; that pipeline is on `old-rust` and this is the whole tool now, so the README no longer says taxonomy synthesis and placement "stay in the Rust crates" but that they are not reimplemented. The `python/` folder that held `syp-paperfetch`, the `uv`-managed arXiv test-set tooling, is gone to make room: nothing in the pipeline called it, and the Rust harness whose measurements it fed is on `old-rust`. `assets/testsets/` keeps the committed manifests as data, with a README saying plainly that nothing builds them any more and that `clustering-eval` never had an artifact at all. `dev.toml` goes with it, having been read only by that package.

  An installed `sypy` from before this points into the deleted `prototype/.venv`. `sypy-path wire` refuses to replace a link it cannot recognise as its own, and it cannot recognise that one, so re-installing means removing `~/.local/bin/sypy` first and re-running `./install.sh`.
- Say when a search was capped, and refuse a negative one. `sypy find` shows 20 matches by default and said nothing when more matched, so a capped result read as the whole answer and the reader stopped looking for a document the library does hold. It now asks for one more row than it will show — which is how the cap is noticed without a second query — and says so on stderr, leaving `--json` stdout parseable. `--limit` is bounded at the CLI, where the input arrives, so `-n -1` is a usage error naming the option rather than a DuckDB binder error surfacing from three layers down.
- Install the agent skill with the tool. `sypy-path wire` now links `prototype/skills/sortyourpapers` into `~/.claude/skills` alongside putting `sypy` on PATH, and `unwire` takes both off — a skill an agent cannot find is no more use than a command that is not on PATH, and the script already owned linking a project artifact into a user directory and refusing to touch one it did not create. Both are symlinks into the project, so editing either takes effect without reinstalling. The link is only made into a skills directory whose parent already exists: a machine with no `~/.claude` will never read a skill put there, and inventing another tool's config folder to hold one is litter, so the install names `SYPY_SKILLS_DIR` instead. A skill of the same name someone else wrote is a warning rather than a refusal, since by then the command is installed. `unwire` does the skill first, so its outright refusal over a `sypy` it did not create cannot leave the skill behind.
- Give the library a read surface a program can use, and a skill over it. `sypy find` matches a query's words against everything a document is described by — id, title, original filename, year, tags, authors, keywords — ANDing words so a second one narrows, and matching in SQL rather than over `all_papers()` because hydrating a paper costs three further queries and a search that hydrates the whole library to discard most of it pays them for nothing. `--json`, on `find` and `list`, prints records carrying the absolute path to each document, its folder, and its notes, since a result whose file the caller cannot open is only half an answer; `sypy note <id> --path` prints where the notes live instead of opening `$EDITOR`, which a caller meaning to write them itself would be left holding open. `prototype/skills/sortyourpapers/` is an agent skill over those three, including which commands spend money at the API or delete the only copy of a document and so are not to be run to answer a question.
- Add `./install.sh`, one installer for macOS and Linux, and make the background service work on both. It finds a Python 3.11+ by trying versioned names before the bare `python3` — a distribution's `python3` is often older than the newest it also ships — then checks the two things that otherwise fail obscurely later: `venv`, which Debian ships as a separate package, and `pdftoppm`, which only scanned documents need and so is a warning naming the package rather than a refusal. `sypy-service` now writes a systemd `--user` unit on Linux as well as a launchd agent on macOS, each the way that platform expects: quoted `ExecStart` so a watched folder with a space in its name survives, `Restart=always` bounded by a start limit so a crashloop stops rather than spins, poppler carried on `PATH`, the rotating log wired up, and a note about `loginctl enable-linger` when a user service would die at logout. Both supervisors are tested with the supervisor command stubbed, so each platform's unit is checked from either.
- Remove the Rust workspace from `main`. `crates/` (six crates), `Cargo.toml`, `Cargo.lock`, and the `rust-skills` agent ruleset are gone, and the root README, `AGENTS.md`, and `docs/architecture.md` now describe what the repository actually holds: the Python tool in `prototype/` and the test-set tooling in `python/`. Nothing is lost — the whole Rust implementation, including the evaluation harness the measurements in `docs/` came from, is preserved on the `old-rust` branch, and those two documents now say so at the top. The `/target` ignores go too, since there is no longer anything to ignore.
- Add `sypy fsck`, which checks the store and the database against each other and, with `--adopt`, gives an orphaned folder a row. Filing writes the file before the row so that an interruption leaves a folder with no row rather than a row with no folder — but nothing could find such a folder, so the document was invisible to every command and, under `--mode move`, the only copy. The claim in `file_paper` that re-ingesting repairs it was wrong and is corrected: ids are minted fresh, so re-ingesting files a second copy.
- Stop three failures that only appeared in the running service. `discover_pdfs` now skips a file that vanishes mid-scan instead of raising, which took the whole watcher down over an ordinary download; reconciliation catches `duckdb.Error` as well as `OSError`, since `duckdb.IOException` is neither and a busy database killed the pass; and the launchd agent is given a PATH containing poppler, without which every scanned PDF failed under the service while working by hand. Watch claims are also made visible only once complete — the previous create-then-fill left a window where a second watcher read an empty claim, took it over, and both ran.
- Fix four data-safety defects found in review. `remove` no longer drops a document's row when its folder could not be deleted, which left the folder on disk with nothing pointing at it; `_unlink` finds a document's link by where it points rather than by recomputing its name, so removing or re-tagging one of two look-alike documents no longer deletes the other's; the filename cap trims the stem so a very long name keeps its extension; and `upsert_many` really is one transaction, so a batch that fails part-way records none of it rather than an arbitrary prefix.
- Cap every test at 60 seconds with `pytest-timeout`'s signal method, so no test can wedge the machine. Several tests drive the watcher, whose loop stays interruptible only because every branch awaits; `asyncio.wait_for` cannot preempt a coroutine that stops yielding, so a change removing one spins at full CPU with nothing able to stop it. Also document the working way to run the tests — the `uv` command in the README requires a tool that need not be installed, and `sypy-path wire` does not install the dev extras.
- Add a registry at `~/.config/sypy/config.toml` declaring named watches — input, library, mode — as the one place that says what this machine watches. `sypy watches` shows what is declared and which are running, `sypy watch [NAME]` runs one, and the service installs from it. Other commands resolve their library from it, so `--library` stops being needed. Two watches sharing a folder are refused when the file is read, naming both, rather than when the second watcher starts. Reporting a watch as running now checks the owner is alive, since a claim outlives a service stopped with SIGTERM.
- Refuse to start a watcher whose input folder or library is already being watched. Two watchers sharing either would file the same document twice, since each decides what the library holds before either writes and the database lock is not held across that gap. Claims are taken atomically, kept in `~/.local/state/sypy` rather than in the watched folders, and taken over when their owner is gone so a crash does not lock a folder out. `sypy-service install` for a different folder is refused too, instead of silently replacing the running agent.
- Cut a long title between words rather than inside one, so a link no longer trails off as `...mammography-and-tomo`. `sypy migrate-store` now also renames stored files the naming rules would spell differently, so the store and the tree keep saying the same thing after a rules change.
- Give each document a real folder in the tree holding a single symlink to its store folder, instead of a bare symlink where the document sits, so browsing a category and opening a document stays in the tree rather than resolving into the store. Those folders can be written into and the tree is disposable, so `sypy tree` now reports any file found living there — dotfiles excluded — and says to move it into the document's folder; it never deletes it.
- Move each document into its own folder in the store, `store/<id>__<Tag>/`, holding the document and whatever is kept beside it; `tree/` becomes one symlink per document pointing at that folder. Notes are now as durable as the document — a note kept only in the tree was lost when the tree was deleted, and the tree is disposable by design. Adds `sypy note` to open a document's notes, `sypy migrate-store` to move a pre-folder library across, and migration `0004_document_folder`. Re-tagging becomes a folder rename and a rebuild becomes one symlink, so neither can lose anything.
- Give each document its own folder in the tree, named after it, so notes and supplements have somewhere to live beside it: `tree/<tags>/<name>/<name>.pdf`. A rebuild now removes only symlinks and the folders they leave empty, where it previously deleted every regular file it found; re-tagging moves the whole folder rather than just the link, so anything filed beside a document follows it.
- Read scanned PDFs instead of refusing them. A document with no text layer has its first pages rendered with `pdftoppm` and read by the model, which writes plain text standing in for the text it does not carry; that text then goes through the ordinary labelling call, so a scan is batched, steered, and named like anything else. Costs one extra request per scanned document. Adds migration `0003_page_image_provenance` recording which documents were read this way. Both scanned PDFs in the test folder now ingest with correct titles, authors, and years.
- Split an ingest pass into phases so the database is only open while it is being used. Hashing, PDF parsing, the model calls, and copying now all run with no connection held, and the database is visited in three short bursts between them; already-held documents are also no longer parsed, since the known-check now happens before parsing rather than after hashing. On twelve 4MB documents this took the share of a pass with the write lock unavailable from 66% to 3%, and the remaining bursts scale with the number of documents rather than their size.
- Stop a running watcher from locking other commands out. DuckDB allows one writing process, so `sypy list`, `scan`, `tree`, and the rest failed outright if run while a pass was in flight — a read-only connection is refused too. The watcher now drops its lock across the model calls, which are the slow part of a pass and touch no database, and a connection that does meet a held lock waits up to 30 seconds instead of failing. Failures that are not contention are still raised at once.
- Reconcile the store at the start of every ingest, so a file edited in place has its hash refreshed before the library decides what it already holds. Previously that needed a manual `sypy scan`, and without it an annotated copy arriving later was ingested a second time. Placed in `ingest_folder` rather than the watch loop, so `sypy ingest` gets it too; the size/mtime gate keeps a quiet library at one stat per document, and a store that cannot be read is reported without stopping the pass.
- Add `sypy scan`, which refreshes the recorded hash of any stored file edited in place. A stale hash is how an annotated copy gets ingested a second time, since the hash is what recognises a document the library already holds. Size and mtime are recorded per file and checked first, so a quiet library costs one stat per document instead of re-reading every file. Adds migration `0002_stored_file_state`, which applies additively to existing libraries.
- Name links from whichever of author, year, and title are known rather than requiring all three, so a document missing only its year keeps a readable name instead of falling back to the opaque store name. Show each new document the category paths the library already uses, so it joins an existing branch instead of inventing a parallel one — an electricity bill and a water bill that previously landed under unrelated top-level folders now share one. Add `sypy remove`, which takes a document's link, file, and record together and asks before deleting.
- Accept documents of any kind rather than assuming academic papers: the prompt now expects bills, receipts, and manuals and categorizes them on their own terms. Replace `--apply` with `--mode preview|copy|move`, where `copy` indexes a folder without rearranging it, and switch filing to `shutil` so a move across filesystems works. Add `prototype/scripts/sypy-service` to run the watcher as a launchd agent. The watcher now drops its DuckDB lock while idle — without it a running service blocked every other `sypy` command, since DuckDB locks per process.
- Redesign filing around a flat content store with a symlink tree over it, and move all paper metadata into DuckDB. Every PDF lives once at `store/<id>__<Tag>__<Tag>.pdf` with a permanent 12-hex id and tags that are not, so re-tagging is a rename plus a moved link; `tree/` holds only relative symlinks named `author_year_title`, falling back to the store name when the model cannot supply all three. DuckDB is the source of truth — filenames and the tree are projections, so `sypy tree` rebuilds the tree exactly and an over-long tag list loses nothing. Ingest's single batched call now also returns title, authors, and year. Adds `sypy list`, `sypy retag`, `sypy tree`, and an `--apply` gate so nothing moves by default. Replaces `ingest.jsonl`.
- Add `prototype/scripts/sypy-path`, which wires the prototype's `sypy` command onto PATH and takes it back off. `wire` builds a virtualenv at `prototype/.venv`, installs the package editable so edits need no reinstall, and links `sypy` into `~/.local/bin`; `unwire` removes that link. Both refuse to touch a `sypy` the script did not create, so an unrelated command of the same name survives. Uses `python3 -m venv` rather than `uv tool install` so it does not require uv.
- Add `prototype/`, a Python prototype of the ingest pipeline and folder watcher: `sypy ingest` reads a folder's PDFs and labels each with keywords and a preliminary category in concurrent LLM batches, and `sypy watch` keeps doing that as papers arrive. Ingest records append to `ingest.jsonl` in the library folder keyed by content hash, so re-running is free and a renamed paper is not paid for twice. Reuses the Rust pipeline's `SYP_*` setting names and mirrors its watcher invariants (scan is the source of truth, 3s settle, library excluded, unchanged input not retried after a failure).
- Run taxonomy synthesis and placement batches concurrently instead of one at a time, capped at 4 in flight like keyword extraction already was. Both stages were round-trip bound rather than token bound — together they were ~21 of a run's ~23 sequential LLM round-trips while carrying ~13% of its tokens. Adds `run_delayed_concurrent_requests_streaming`, which hands each result back as it lands so resumable batch progress is still persisted per batch, and unifies the per-stage concurrency ceiling into one `MAX_CONCURRENT_BATCH_REQUESTS`.
- Remove `Tomorrow.md`. Its refactor plan was written around the `syp`/`syptui` two-frontend split and is superseded by the move to a non-TUI watcher; the CLI-side crate extraction it called for (`syp-ai`, `syp-library`, `syp-workflow`) already landed.
- Remove the `syptui` TUI frontend and its `ratatui`/`crossterm`/`tui-tree-widget` dependencies, leaving `syp` as the only binary, as the project moves to a non-TUI watcher. The TUI-only `run_debug_tui` seeded-run fixture in `syp-workflow` goes with it, `docs/ui-milestones.md` moves to `docs/archive/`, and the pre-removal tree stays reachable at the `archive/syptui` tag.
- Write `placement_mode = "allow-new"` and `use_current_folder_tree = true` into the config `syp watch init` generates, so a watched folder behaves as the growing library it is: a paper on a subject the library does not cover can open its own folder, and taxonomy merge sees the existing tree instead of inventing near-duplicates of it. Global run defaults are unchanged.
- Add an offline clustering bench that scores candidate grouping strategies against the curated set's arXiv labels without an API key, plus `docs/clustering_bench.md` recording what it measured: term-overlap clustering tops out near ARI 0.20 even when handed the true label count, choosing how many groups exist matters more than any threshold tuning, and v-measure moves opposite to ARI as grouping fragments. Embedding-based grouping is implemented and gated on `SYP_API_KEY`.
- Report the placement score and margin spread a run recorded in `syp eval`, along with the gates that would defer the weakest fifth of decisions to the model, so placement thresholds can be set from measured distributions instead of round numbers.
- Split the reference-retrieval bar from the placement decision gate, and stop a target profiled only by its own folder name from winning a placement on embeddings alone.
- Group per-paper category labels into concepts before synthesis and batch each synthesis request by subject, replacing exact-string counting and positional chunking of an alphabetical list.
- Add a balanced `clustering-eval` curated set policy and `syp eval`, which scores a run's folders against arXiv labels with adjusted rand index, v-measure, homogeneity, completeness, and purity.
- Add an experimental `genai`-backed LLM adapter behind the existing `LlmClient` trait, selected with `SYP_LLM_BACKEND=genai`, covering chat, JSON-schema output, vision, and embeddings for all three providers so the two backends can be compared without touching any call site.
- Read scanned PDFs that have no text layer by rendering their first pages with `pdftoppm` and asking the configured chat model to describe them, so image-only papers get sorted instead of failing extraction; the run reports how many were read this way, and `PaperText` records which papers came from page images.
- Add `LlmClient::chat_with_images` with request bodies for Gemini inline data, both OpenAI APIs, and Ollama, so page images reach whichever provider is configured.
- Distinguish "this PDF has no text layer" from "this PDF could not be read" in the extractors, which previously reported both as the same extraction failure.
- Update the default chat models to `gemini-3.7-flash` for Gemini and `gpt-5.6-terra` for OpenAI, including the generated config template and the OpenAI live-test fallback.
- Default the chat model from the resolved provider instead of always falling back to the Gemini model, so `--llm-provider ollama` or `openai` with no model no longer sends `gemini-3-flash-preview` to the wrong API; `DEFAULT_LLM_MODEL` is replaced by per-provider constants and `default_llm_model(provider)`.
- Carry the new provider's default model into the TUI run form when cycling the provider field, leaving a model the user typed untouched.
- Prompt for watch folder settings in `syp watch init` when it runs in a terminal, using `cliclack` for the library folder, scan depth, model backend, model, and API key environment variable; piped or CI runs skip the questions and write the defaults, and rerunning in a terminal offers to overwrite instead of failing after the questions are answered.
- Point `syp watch init` at the current directory when `--input` is omitted, instead of the configured input folder, so `SYP_INPUT` or an `input` key in the XDG config cannot redirect setup away from the folder the user is standing in.
- Add `syp watch init`, which writes a `syp.toml` into the folder being watched so a watch folder carries its own library location, scan depth, and model settings without touching the global XDG config or the TUI; the folder layer sits between `SYP_*` environment variables and the XDG config, and cannot set `input` because the file's own folder determines it.
- Clean `.` and `..` out of configured input and output paths when they are made absolute, so config files and watcher logs no longer show paths like `/inbox/./sorted`.
- Add `syp watch`, a long-running watcher that organizes the input folder whenever new PDFs settle in it: filesystem events wake a rescan, the folder must stop changing before a run starts, PDFs already inside the output library are ignored, and an unchanged input is never re-run after a failure.
- Add `terminal::install_unattended_backend` so unattended runs answer taxonomy review prompts with their defaults instead of blocking on stdin even when stdin is a terminal.
- Split the TUI run form into essential and advanced tiers: the default form now shows 11 fields (paths, scope, depth, placement policy, LLM/API, apply, run) and `a` reveals the 13 tuning fields, which still feed the run config and `s` save whether shown or not.
- Replace the run form's magic field indices with a typed `RunField` table that owns each field's label, help text, edit kind, tier, and layout position, so navigation, rendering, and validation no longer keep parallel index lists.
- Merge the identical `EnvConfig` and `FileConfig` structs into a single `ConfigLayer`, so the env and XDG override layers share one 34-field definition instead of two.
- Move the oversized `syptui::tui::mod` test suite into `syptui::tui::tests` so the main TUI module now stays focused on bootstrap and event-loop runtime logic as planned in `Tomorrow.md`.
- Extract reusable AI, library-domain, and workflow logic into new `syp-ai`, `syp-library`, and `syp-workflow` crates, and reduce `syp-core` to a compatibility facade that keeps the existing CLI and TUI import surface stable while the architecture shifts under it.
- Reflow the split placement runtime call sites and validation branches so the post-extraction `syp-core` placement modules stay consistently formatted before larger crate-boundary moves build on them.
- Split `syptui::tui::render` into `header`, `home`, `extract`, `operation`, `overlay`, `review`, and shared `layout` modules so screen-specific drawing logic stops sharing one 1,200-line file while preserving the current TUI flow and test surface.
- Split placement runtime orchestration into `engine`, `embedding`, `scoring`, `llm_tiebreak`, and `progress` submodules so embedding-backed ranking, JSON retry handling, and saved-batch resume validation can evolve independently without another 1,100-line runtime file.
- Split `syp-core::app`, `syp-core::session::commands`, and `syptui::tui::forms::run_form` into focused submodules so the largest remaining control-flow and TUI configuration files stop accumulating unrelated responsibilities ahead of the broader Tomorrow.md architecture extraction.
- Remove volatile local run IDs and reference DB path details from `docs/embedding_comparison.md` so the comparison stays useful across machines and reruns without baking in host-specific values.
- Split `syp-core` session workspace internals into `types`, `paths`, `store`, `queries`, and `cleanup` modules so run persistence, inspection, and deletion logic can evolve independently without changing current CLI or TUI behavior.
- Add an embedding-guided taxonomy path backed by DuckDB reference indexing, nearest-label retrieval, dedicated embedding config, and the new `syp reference index` command.
- Add a shared `dev.toml` test-set cache root so Python materialization and Rust manifest/state readers use the same repo-relative PDF and metadata cache.
- Remove legacy `assets/papers/` sample PDFs now that curated test-set artifacts and shared cache-backed materialization cover the dataset workflow.
- Switch the default Gemini embedding model to `gemini-embedding-2-preview` for embedding-guided taxonomy runs and generated config templates.
- Add an ignored live Gemini embedding test so real API validation can be run explicitly without affecting the default test suite.
- Add `docs/embedding_comparison.md` capturing the curated-set embedding-guided vs llm-only taxonomy diff and per-paper category changes with arXiv metadata.
- Add embedding-primary placement assistance that builds DuckDB-backed category centroids from reference embeddings, uses LLM tie-breaks only for ambiguous files, and saves per-run placement evidence.
- Rename the DuckDB storage crate from `paperdb` to `paper-db`, update workspace/dependency references, and switch the default database filename to `paper-db.duckdb`.
- Add `Tomorrow.md` with a maintainers' refactor plan for decoupling `syp-core`, shrinking oversized modules, and evolving the app toward a scalable library-centered LLM and embedding platform.
